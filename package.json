{
  "name": "vocabuki-eventcalendar",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "site": "node build.mjs && node site.mjs",
    "test": "node build.mjs --check && node test/site.test.mjs && node mcp/test/run-tests.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  }
}
